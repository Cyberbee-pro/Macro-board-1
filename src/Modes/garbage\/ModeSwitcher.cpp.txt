#include <Arduino.h>
#include <PINS.h>
#include <POT.h>
#include <ModeDisp.h>
#define setCurSrl() (curr_srl = Current_config->getSerial())

bool Current_Butt_state;
bool Prev_Butt_state = HIGH;
// int led_array_state = 0;



mode *Current_config = &def;
int curr_srl = 1;


void SearchNset(int target_srl){
    bool found = false;
    for(mode* temp : mode_list_vec){
        if(temp->getSerial()==target_srl){
            Current_config = temp;
            curr_srl = setCurSrl();
            found = true;
            return;
        }
    }
    
    if(found!=true){
    Serial.println("Target Serial not found. . . . . . .");
    }

}




void Mode_switch(){

    setCurSrl();
    int maxCount = mode::getCount() - 1;
    Current_Butt_state = digitalRead(BUTTON_TOGGLE_MACRO);
    // if condition had a Prev_Butt_state checkpoint but its not needed (working even without it)
    if(Current_Butt_state == LOW && Prev_Butt_state == HIGH){
        Serial.println("Macro Button presed");
        if(curr_srl < maxCount){
            SearchNset(curr_srl + 1);
        }else if(curr_srl == maxCount){
            SearchNset(1);
        }

        Serial.print("\n||Current Macro : ");
        Serial.print((Current_config->getName()).c_str());
        Serial.print("||Serial : ");
        Serial.print(Current_config->getSerial());
        Serial.println("");
        delay(100);
    }
    Prev_Butt_state = Current_Butt_state;
}



void Mode_run(){
    Current_config->call();
}

void Mode_show(){
    
    setCurSrl();
    if (curr_srl < 1 || curr_srl > 3) {
        curr_srl = 1; // Brute force recovery back to Default profile
    }

    // 🟢 Step 2: Clear all three status LEDs cleanly every iteration
    digitalWrite(LED[0], LOW);
    digitalWrite(LED[1], LOW);
    digitalWrite(LED[2], LOW);

    // 🟢 Step 3: Run safe array index writes mapping 1->3 directly to index 0->2
    int Cur_LED_INDEX = curr_srl - 1;
    if (Cur_LED_INDEX >= 0 && Cur_LED_INDEX < 3) {
        digitalWrite(LED[Cur_LED_INDEX], HIGH);
    }
    
    
    // // 🟢 Safety Guard: Check if the config pointer got corrupted or dropped
    // // if (Current_config == nullptr) {
    // //     Serial.println("CRITICAL ERROR: Current_config is NULL!");
    // //     return; 
    // // }

    // Serial.println("TRY MODE");

    // setCurSrl();
    // // for(int i = 0; i < 3; i++){
    // //     digitalWrite(LED[i], LOW);
    // // }

    // // if(curr_srl > 3){
    // //     Mode_show_bin();
    // //     return;
    // // }

    // int Cur_LED_INDEX = curr_srl - 1;
    // if (Cur_LED_INDEX >= 0 && Cur_LED_INDEX < 3) {
    //     digitalWrite(LED[Cur_LED_INDEX], HIGH);
    // }
}


void Mode_show_bin(){
    setCurSrl();
    // File: src/Modes/ModeSwitcher.cpp

// 🟢 FIX: Catch out-of-bounds upper limits AND negative/zero memory noise
if (curr_srl > 6 || curr_srl <= 0) {
    Serial.printf("FATAL ERROR: INVALID MACRO SERIAL DETECTED (%d)\n", curr_srl);
    return;
}

    for(int i = 0; i<3;i++){
        digitalWrite(LED[i],LOW);
    }
//     curr_srl = setCurSrl();
//     led_array_state = dec2bin(curr_srl);
//     for(int i = 2;led_array_state>0;i--){
//         digitalWrite(LED[i],led_array_state%10);
//         led_array_state/=10;
//     }

    digitalWrite(LED[2], (curr_srl & 0x01) ? HIGH : LOW);
    digitalWrite(LED[1], (curr_srl & 0x02) ? HIGH : LOW);
    digitalWrite(LED[0], (curr_srl & 0x04) ? HIGH : LOW);

}